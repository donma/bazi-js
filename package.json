{
  "name": "bazijs",
  "version": "1.0.2",
  "description": "Browser-first, zero-dependency BaZi (Four Pillars) calculation SDK in JavaScript.",
  "main": "dist/bazi-sdk.js",
  "browser": "dist/bazi-sdk.js",
  "module": "dist/bazi-sdk.esm.js",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node tests/run.js",
    "validate": "node scripts/validate-external.js",
    "gen-golden": "node scripts/generate-golden.js",
    "check:demo": "node scripts/check-demo.js",
    "validate:taxonomy": "node scripts/validate-taxonomy.js",
    "validate:references": "node scripts/validate-references.js",
    "generate:reference-docs": "node scripts/generate-reference-docs.js",
    "check:reference-docs": "node scripts/generate-reference-docs.js --check",
    "generate:coverage": "node scripts/generate-coverage.js",
    "validate:coverage": "node scripts/generate-coverage.js --check",
    "ci": "npm test && npm run validate && npm run validate:taxonomy && npm run validate:references && npm run generate:reference-docs && npm run generate:coverage && npm run build && npm run check:demo && npm run check:reference-docs && npm run validate:coverage"
  },
  "files": [
    "VERSIONS.md",
    "dist",
    "src",
    "sources",
    "profiles",
    "schemas",
    "validation",
    "docs"
  ],
  "keywords": [
    "bazi",
    "four-pillars",
    "bazi-sdk",
    "astrology",
    "lunar-calendar",
    "solar-terms",
    "shensha"
  ],
  "author": "BaziJS Contributors",
  "license": "Apache-2.0",
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
